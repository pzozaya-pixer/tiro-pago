services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      APP_URL: ${APP_URL:-http://localhost:8080}
      NODE_ENV: production
    expose:
      - "80"
    restart: unless-stopped
